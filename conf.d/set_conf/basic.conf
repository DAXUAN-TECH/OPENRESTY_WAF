# 基础配置参数
# 注意：此文件保持在项目目录，不复制到系统目录

# 注意：性能优化相关配置已移至 performance.conf
# 此文件保留基础配置，避免与 performance.conf 冲突

# MIME 类型哈希表大小（优化查找性能）
types_hash_max_size 4096;
server_names_hash_bucket_size 128;
server_names_hash_max_size 2048;

