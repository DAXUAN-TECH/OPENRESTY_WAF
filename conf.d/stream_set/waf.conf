# Stream WAF 配置文件
# 注意：此文件保持在项目目录，不复制到系统目录
# nginx.conf 中的 include 路径会指向项目目录的 conf.d/stream_set

# 注意：WAF 共享内存（waf_cache 和 waf_log_buffer）已在 HTTP 块中定义
# lua_shared_dict 是进程级别的，Stream 块可以直接使用 HTTP 块中定义的共享内存
# 不需要在 Stream 块中重复定义，否则会导致 "already declared" 错误

