# ============================================
# 代理配置: ssh (ID: 19)
# 类型: TCP
# 自动生成，请勿手动修改
# ============================================

server {
    listen 2222;
    proxy_pass stream_upstream_19;
    proxy_timeout 60s;
    proxy_connect_timeout 60s;
}

